function rootPath=isetlfmRootPath()
% Return the path to the iset-lfm root iset directory
%
% This function must reside in the directory at the base of the
% iset-lfm directory structure.  It is used to determine the
% location of various sub-directories.
%
% Example:
%   fullfile(iset-lfm,'data')

rootPath=which('isetlfmRootPath');

rootPath = fileparts(rootPath);

end